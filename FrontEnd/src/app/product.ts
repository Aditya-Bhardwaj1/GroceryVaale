export class Product {
  productId?: number;
  productName?: string;
  description?: string;
  unitPrice?: number;
  unitsInStock?: number;
  discontinued?: boolean;
  categoryId?: number;
  productImage?: string;
}
